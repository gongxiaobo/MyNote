using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testOpenBubble : MonoBehaviour {

     //// Use this for initialization
     void Start()
     {
          // I_backToSelect t_back = S_SceneMagT1.Instance;
          ////训练返回选择场景
          // t_back.fni_TrainBackSelect();
          // //结束考试
          // t_back.fni_StopTest();
          // //考试返回选择场景
          // t_back.fni_TestBackSelect();
     }
	
     //// Update is called once per frame
     //void Update () {
     //     if (Input.GetKeyDown(KeyCode.S))
     //     {
     //          //冒泡显示
     //          S_SceneMagT1.Instance.fn_BubblesShow(true);
     //          //整体介绍显示
     //          S_SceneMagT1.Instance.fn_IntroduceShow(true);
     //          ////设置音量（0-1）
     //          //S_SoundComSingle.Instance.fn_setAllSoundVolum(0.5f);
     //          ////静音和打开声音，true为静音
     //          //S_SoundComSingle.Instance.fn_shutSound(true);
     //     }
     //     if (Input.GetKeyDown(KeyCode.D))
     //     {
     //          S_SceneMagT1.Instance.fn_BubblesShow(false);
     //          S_SceneMagT1.Instance.fn_IntroduceShow(false);
     //     }

     //}
    
}
